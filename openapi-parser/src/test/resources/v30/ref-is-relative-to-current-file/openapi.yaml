openapi: 3.0.2
info:
  title: API
  version: 1.0.0

paths:
  /foo:
    get:
      responses:
        '200':
          description: none
          content:
            application/json:
              schema:
                $ref: 'schemas/foo.yaml#/Foo'

